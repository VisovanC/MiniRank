services:
  app:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
  test:
    build:
      context: .
      target: test
    profiles: ["test"]
    command: ["php", "vendor/bin/phpunit"]